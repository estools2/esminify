function c() {
  console.log(1)
}
filter.on('filter_query', a => ComCenter.getComIdsByType('profile').forEach(b => ComCenter.getComById(b).emit('filter_query', a)));
let a = (a, b) => a(b);
let b = (a, b) => a + b