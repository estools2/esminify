function callremote(path, optoins, callback) {
  let arr = [];
  ipList.forEach(function (ip) {
    if (ip.indexOf(':') > 0) {
      let arr = ip.split(':');
    }
    arr.push(function (cb) {});
  });
}